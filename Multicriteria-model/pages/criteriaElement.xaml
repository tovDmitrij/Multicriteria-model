<UserControl x:Class="Multicriteria_model.criteriaElement"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
              xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
              TextElement.Foreground="{DynamicResource MaterialDesignBody}"
              TextElement.FontWeight="Regular"
              TextElement.FontSize="13"
              TextOptions.TextFormattingMode="Ideal" 
              TextOptions.TextRenderingMode="Auto"
              FontFamily="{DynamicResource MaterialDesignFont}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Transparent">
        <DockPanel>
            <Border Padding="0,0,0,10">
                <StackPanel Orientation="Vertical" Background="#FF003238" Margin="10,10,0,0">
                    <Label Background="#FF00262B" Foreground="#DDD2D2D2" Margin="10px" HorizontalContentAlignment="Center" FontSize="20" Content="%NAME%"/>
                    <StackPanel Margin="5,0,5,0" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Label Foreground="#DDD2D2D2" FontSize="14" Content="Не %LOWER OR UPPER%"/>
                        <TextBox PreviewTextInput="PreviewValueInput" x:Name="criteriaValue" Foreground="#DDD2D2D2" FontSize="14" materialDesign:HintAssist.Hint="Введите значение" Cursor="Arrow"/>
                    </StackPanel>
                    <Label Background="#FF00262B" Foreground="#DDD2D2D2" Margin="10px" HorizontalAlignment="Center" FontSize="18" Content="ПРИОРИТЕТ"/>
                    <StackPanel Margin="5,0,5,0" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Label Foreground="#DDD2D2D2" FontSize="14" Content="№"/>
                        <TextBox PreviewTextInput="PreviewPriorityInput" x:Name="pricePriority" Foreground="#DDD2D2D2" FontSize="14" materialDesign:HintAssist.Hint="Введите приоритет"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </DockPanel>
    </Grid>
</UserControl>